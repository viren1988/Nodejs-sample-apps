{
  "name": "sample-k8s-app",
  "version": "1.0.0",
  "description": "Sample application for Kubernetes multi-environment deployment",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
